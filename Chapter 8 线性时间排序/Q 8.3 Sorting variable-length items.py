# 思考题8.3：（变长数据项的排序）

# 8.3(a)：给定一个整数数组，其中不同的整数所包含的数字的位数可能不同，但该数组中，所有整数中包含的总数字位数为 n。设计一个算法，使其可以在 O(n) 时间内
#         对该数组进行排序。

# 8.3(b)：给定一个字符串数组，其中不同的字符串所包含的字符串可能不同。但该数组中，所有字符串中的总字符个数为 n。设计一个算法，使其可以在 O(n) 时间内
#         对该数组进行排序。（注意：此处的顺序是指标准的字典序，例如 a < ab < b）

